# MenuBoard Editor — serve the SPA static files and PHP API from the custom directory.
# Included via IncludeOptional /etc/apache2/conf.d/*.conf (outside VirtualHost is valid for Alias).
Alias /menuboard-editor/ /var/www/cms/custom/MenuBoard/editor/
<Directory /var/www/cms/custom/MenuBoard/editor/>
    Options -Indexes +FollowSymLinks -MultiViews
    AllowOverride None
    Require all granted

    # Pass Authorization header through to PHP (needed for POS Bearer token auth).
    CGIPassAuth On
    RewriteEngine On
    RewriteCond %{HTTP:Authorization} .+
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]
</Directory>

# Note: /menuboard/v1/ REST routes are injected into /var/www/cms/web/.htaccess
# by menuboard-entrypoint.sh on startup, because Xibo's VirtualHost processes
# that .htaccess before server-level conf.d rules can fire.
